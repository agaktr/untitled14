{% trans_default_domain 'login' %}
<h2 class="text-xl font-semibold mt-2 pt-1 mb-0">Verify Email</h2>

<p class="text-sm font-semibold mt-2 pt-1 mb-0">
    Visit {{ app.user.email }} and validate your email by clicking in the link.
    Then you can log in to the dashboard.
</p>

<p class="text-sm font-semibold mt-2 pt-1 mb-0">
    {% trans %}That's all.{% endtrans %}
    <a
            href="{{ path('app_home') }}"
            class="text-red-600 hover:text-red-700 focus:text-red-700 transition duration-200 ease-in-out"
    >{% trans %}Back to Home{% endtrans %}</a
    >
</p>